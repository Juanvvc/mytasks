import Vue from 'vue'
import Vuetify from 'vuetify'
import 'vuetify/dist/vuetify.min.css'

Vue.use(Vuetify, {
    // Black and orange
    //theme: { primary: "#344955", secondary: "#f9aa33", accent: "#FF7043", error: "#f44336", warning: "#FBC02D", info: "#2196f3", success: "#4caf50" }
    // Blask and green
    theme: { primary: "#565656", secondary: "#00897b", accent: "#FBC02D", error: "#f44336", warning: "#FBC02D", info: "#2196f3", success: "#4caf50" }
})
